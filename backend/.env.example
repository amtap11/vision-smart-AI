# Server Configuration
PORT=3001
NODE_ENV=development

# Database Configuration
# For production: Use encrypted connection string with SSL
# Example: postgresql://user:pass@host:5432/db?sslmode=require
DATABASE_URL=postgresql://postgres:password@localhost:5432/vision_smart_ai

# API Keys
# CRITICAL: Never commit actual API keys to version control
GEMINI_API_KEY=your_gemini_api_key_here

# JWT Configuration
# CRITICAL: Generate a strong random secret for production
# Use: openssl rand -base64 64
# Minimum 64 characters recommended
JWT_SECRET=your_super_secret_jwt_key_here_change_in_production_minimum_64_chars

# JWT Expiration Times
# Access token: Short-lived (1 hour recommended)
# Refresh token: Longer-lived (7 days recommended)
JWT_EXPIRATION=1h
JWT_REFRESH_EXPIRATION=7d

# CORS Configuration
# In production, set to your actual frontend domain(s)
FRONTEND_URL=http://localhost:3000

# Security Configuration (Optional - defaults provided)
# Rate limiting can be adjusted based on your needs
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100
# AUTH_RATE_LIMIT_MAX=5
# AI_RATE_LIMIT_MAX=20

# Logging Configuration (Optional)
# LOG_LEVEL=info
# AUDIT_LOG_RETENTION_DAYS=90
