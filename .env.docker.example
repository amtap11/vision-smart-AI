# ============================================
# Docker Environment Configuration
# ============================================
# Copy this file to .env.docker and fill in your values
# Usage: docker-compose --env-file .env.docker up

# ============================================
# Backend Environment Variables
# ============================================

# JWT Configuration (REQUIRED - change in production!)
JWT_SECRET=your_super_secret_jwt_key_here_change_in_production_minimum_32_characters
JWT_EXPIRES_IN=7d

# Gemini AI API Key (REQUIRED)
GEMINI_API_KEY=your_gemini_api_key_here

# ============================================
# Frontend Environment Variables
# ============================================

# API URL for frontend to connect to backend
# For local development: http://localhost:3001
# For production: https://api.yourdomain.com
VITE_API_URL=http://localhost:3001

# ============================================
# Database Environment Variables
# ============================================
# These are already configured in docker-compose.yml
# Only override if you need different values

# POSTGRES_DB=vision_smart_ai
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=password

# ============================================
# Image Tagging (for CI/CD)
# ============================================

# Docker image registry (e.g., docker.io, ghcr.io, gcr.io)
DOCKER_REGISTRY=docker.io

# Docker image namespace (your username or organization)
DOCKER_NAMESPACE=your-username

# Image version tag (default: latest)
IMAGE_TAG=latest

# Alternative: Use git commit hash for versioning
# IMAGE_TAG=$(git rev-parse --short HEAD)
